import 'package:flutter/material.dart';

class UbicationScreen extends StatelessWidget {
  const UbicationScreen({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pagina para buscar'),
    );
  }
}